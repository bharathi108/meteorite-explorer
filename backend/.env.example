# Environment-specific config lives in .env.development or .env.production (see *.example files).
# APP_ENV selects which file to load (default: development).
#
# Quick start (dev):
#   cp .env.development.example .env.development
#   # edit .env.development with your OpenAI key
#   uvicorn app.main:app --reload

APP_ENV=development
