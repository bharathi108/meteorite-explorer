# Vite loads env files by mode:
#   .env.development  → npm run dev
#   .env.production   → npm run build (copy from .env.production.example)
#   .env.local        → optional overrides (gitignored)

VITE_API_URL=/api
